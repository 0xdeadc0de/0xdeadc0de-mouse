#-------------------------------------------------------------------72->
# static helper methods are defined below

def print_verbose(text, category):
    if category in [
        # toggle on/off for verbose printing
        #"released"
    ]:
        print(text)
